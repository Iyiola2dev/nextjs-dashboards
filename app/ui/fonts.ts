import { Inter, Lusitana,Bellefair } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const manrope = Bellefair({
    subsets: ["latin"],
    weight: "400"
});
export const lusitana = Lusitana({ subsets: ["latin"], weight: "400" });
